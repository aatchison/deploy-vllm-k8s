# Run E2B and E4B simultaneously, one on each mig-2g.48gb slice (GPU 0).
# Each gets its own deployment, service, and NodePort.
#
#   E2B endpoint: http://<node>:30801/v1
#   E4B endpoint: http://<node>:30802/v1

---
# --- E2B ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-e2b
  namespace: vllm
  labels:
    app: vllm-e2b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm-e2b
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: vllm-e2b
        model: gemma-4-E2B-it
    spec:
      containers:
      - name: vllm
        image: docker.io/library/vllm-gemma4:local
        imagePullPolicy: Never
        args:
        - "--model"
        - "bg-digitalservices/Gemma-4-E2B-it-NVFP4"
        - "--quantization"
        - "nvfp4"
        - "--port"
        - "8000"
        - "--max-model-len"
        - "32768"
        - "--gpu-memory-utilization"
        - "0.90"
        - "--enable-auto-tool-choice"
        - "--tool-call-parser"
        - "gemma4"
        env:
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-token
              key: token
        - name: HF_HOME
          value: /models/huggingface
        ports:
        - name: http
          containerPort: 8000
          protocol: TCP
        resources:
          limits:
            nvidia.com/mig-2g.48gb: "1"
        volumeMounts:
        - name: model-storage
          mountPath: /models
        - name: shm
          mountPath: /dev/shm
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 300
          periodSeconds: 30
          failureThreshold: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 10
          failureThreshold: 30
      tolerations:
      - key: nvidia.com/mig-2g.48gb
        operator: Exists
        effect: NoSchedule
      volumes:
      - name: model-storage
        persistentVolumeClaim:
          claimName: vllm-models-pvc
      - name: shm
        emptyDir:
          medium: Memory
          sizeLimit: 8Gi

---
apiVersion: v1
kind: Service
metadata:
  name: vllm-e2b-svc
  namespace: vllm
spec:
  selector:
    app: vllm-e2b
  type: NodePort
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    nodePort: 30801

---
# --- E4B ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-e4b
  namespace: vllm
  labels:
    app: vllm-e4b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm-e4b
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: vllm-e4b
        model: gemma-4-E4B-it
    spec:
      containers:
      - name: vllm
        image: docker.io/library/vllm-gemma4:local
        imagePullPolicy: Never
        args:
        - "--model"
        - "google/gemma-4-E4B-it"
        - "--dtype"
        - "bfloat16"
        - "--port"
        - "8000"
        - "--max-model-len"
        - "32768"
        - "--gpu-memory-utilization"
        - "0.90"
        - "--enable-auto-tool-choice"
        - "--tool-call-parser"
        - "gemma4"
        env:
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-token
              key: token
        - name: HF_HOME
          value: /models/huggingface
        ports:
        - name: http
          containerPort: 8000
          protocol: TCP
        resources:
          limits:
            nvidia.com/mig-2g.48gb: "1"
        volumeMounts:
        - name: model-storage
          mountPath: /models
        - name: shm
          mountPath: /dev/shm
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 480
          periodSeconds: 30
          failureThreshold: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 90
          periodSeconds: 10
          failureThreshold: 30
      tolerations:
      - key: nvidia.com/mig-2g.48gb
        operator: Exists
        effect: NoSchedule
      volumes:
      - name: model-storage
        persistentVolumeClaim:
          claimName: vllm-models-pvc
      - name: shm
        emptyDir:
          medium: Memory
          sizeLimit: 8Gi

---
apiVersion: v1
kind: Service
metadata:
  name: vllm-e4b-svc
  namespace: vllm
spec:
  selector:
    app: vllm-e4b
  type: NodePort
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    nodePort: 30802

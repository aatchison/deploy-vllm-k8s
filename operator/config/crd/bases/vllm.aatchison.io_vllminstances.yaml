---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: vllminstances.vllm.aatchison.io
spec:
  group: vllm.aatchison.io
  names:
    kind: VLLMInstance
    listKind: VLLMInstanceList
    plural: vllminstances
    shortNames:
    - vllm
    singular: vllminstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.endpoint
      name: Endpoint
      type: string
    - jsonPath: .spec.presetRef.name
      name: Preset
      type: string
    - jsonPath: .spec.nodePort
      name: NodePort
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VLLMInstance is a declarative vLLM Deployment + NodePort Service
          bundle.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VLLMInstanceSpec is the desired state of a single vLLM deployment.
            properties:
              hfToken:
                description: SecretKeySelector selects a key of a Secret.
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              nodePort:
                description: |-
                  NodePort to expose on every cluster node. Must be in 30000-32767.
                  If omitted, Kubernetes auto-assigns a free port from the NodePort range.
                format: int32
                maximum: 32767
                minimum: 30000
                type: integer
              overrides:
                description: |-
                  ModelConfigOverrides mirrors ModelPresetSpec field-for-field; every field is a pointer —
                  non-nil means override. Probe overrides replace the whole ProbeConfig struct.
                properties:
                  dtype:
                    type: string
                  enableAutoToolChoice:
                    type: boolean
                  gpuMemoryUtilization:
                    type: string
                  image:
                    type: string
                  imagePullPolicy:
                    type: string
                  livenessProbe:
                    description: |-
                      ProbeConfig is the tunable subset of a k8s probe.
                      Liveness periodSeconds=30 and failureThreshold=10 are hardcoded constants in the builder.
                    properties:
                      failureThreshold:
                        format: int32
                        minimum: 1
                        type: integer
                      initialDelaySeconds:
                        format: int32
                        minimum: 0
                        type: integer
                      periodSeconds:
                        format: int32
                        minimum: 1
                        type: integer
                    required:
                    - failureThreshold
                    - initialDelaySeconds
                    - periodSeconds
                    type: object
                  maxModelLen:
                    format: int32
                    type: integer
                  migResource:
                    type: string
                  migResourceCount:
                    format: int32
                    type: integer
                  modelID:
                    type: string
                  progressDeadlineSeconds:
                    format: int32
                    type: integer
                  quantization:
                    type: string
                  readinessProbe:
                    description: |-
                      ProbeConfig is the tunable subset of a k8s probe.
                      Liveness periodSeconds=30 and failureThreshold=10 are hardcoded constants in the builder.
                    properties:
                      failureThreshold:
                        format: int32
                        minimum: 1
                        type: integer
                      initialDelaySeconds:
                        format: int32
                        minimum: 0
                        type: integer
                      periodSeconds:
                        format: int32
                        minimum: 1
                        type: integer
                    required:
                    - failureThreshold
                    - initialDelaySeconds
                    - periodSeconds
                    type: object
                  servedModelName:
                    type: string
                  shmSizeLimit:
                    type: string
                  startupProbe:
                    description: |-
                      ProbeConfig is the tunable subset of a k8s probe.
                      Liveness periodSeconds=30 and failureThreshold=10 are hardcoded constants in the builder.
                    properties:
                      failureThreshold:
                        format: int32
                        minimum: 1
                        type: integer
                      initialDelaySeconds:
                        format: int32
                        minimum: 0
                        type: integer
                      periodSeconds:
                        format: int32
                        minimum: 1
                        type: integer
                    required:
                    - failureThreshold
                    - initialDelaySeconds
                    - periodSeconds
                    type: object
                  tensorParallelSize:
                    format: int32
                    type: integer
                  toolCallParser:
                    type: string
                type: object
              presetRef:
                description: PresetReference names a ModelPreset in the same namespace.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              pvcName:
                type: string
              replicas:
                format: int32
                maximum: 1
                minimum: 0
                type: integer
            required:
            - hfToken
            - pvcName
            type: object
            x-kubernetes-validations:
            - message: presetRef or (overrides.modelID, overrides.migResource, overrides.maxModelLen)
                must be set
              rule: has(self.presetRef) || (has(self.overrides) && has(self.overrides.modelID)
                && has(self.overrides.migResource) && has(self.overrides.maxModelLen))
            - message: overrides.tensorParallelSize > 1 requires overrides.migResourceCount
                == tensorParallelSize
              rule: '!has(self.overrides) || !has(self.overrides.tensorParallelSize)
                || self.overrides.tensorParallelSize <= 1 || (has(self.overrides.migResourceCount)
                && self.overrides.migResourceCount == self.overrides.tensorParallelSize)'
            - message: replicas must be 0 or 1 (MIG slice cannot host multiple pods)
              rule: '!has(self.replicas) || self.replicas <= 1'
          status:
            description: VLLMInstanceStatus reflects the observed state.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deploymentName:
                type: string
              endpoint:
                type: string
              observedGeneration:
                format: int64
                type: integer
              readyReplicas:
                format: int32
                type: integer
              resolvedConfigHash:
                type: string
              serviceName:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
